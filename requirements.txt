# PyTorch is installed separately so you can pick the right CUDA build:
#   pip install torch --index-url https://download.pytorch.org/whl/cu132
# (see scripts/setup_env.sh; tested with torch 2.13.0, any recent 2.x works)
transformers==5.14.1
numpy==2.4.4
scipy==1.18.0
tqdm==4.68.4
PyYAML==6.0.3
py7zr==1.1.3
